
<%= form_for @review do |f| %>

	<div class="field">
		<%= f.label :comment %><br>
		<%= f.text_area :comment, size: "50x10" %><br>
		</div>

	 	<div class="rating">
	    <%= f.label :rating %><br>
	    <%= f.text_field :rating %>
	    </div>
		
		<div class="actions">
		<%= f.submit %>	
	</div>
<% end %>
